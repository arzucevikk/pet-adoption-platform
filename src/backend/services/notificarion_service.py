def notify_shelter(shelter_id, message):
    # Simulated Notification Service (Email/SMS)
    print(f"[NotificationService] To Shelter({shelter_id}): {message}")

def notify_user(user_id, message):
    print(f"[NotificationService] To User({user_id}): {message}")